{
  "name": "stun-only-voice-call-with-60-minute-limit",
  "version": "1.0.0",
  "description": "STUN-only voice call app",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dist": "electron-builder --win"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.0"
  },
  "build": {
    "appId": "com.example.stunvoicecall",
    "productName": "STUN Voice Call",
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "icon": "build/icon.ico"
    }
  }
}
